Scenario: A trainer is viewing the stat breakdown page

Given that a trainer is logged in

When the trainer accesses the stat breakdown page,

Then the page loads

And the trainer can view all of the relevant stats.